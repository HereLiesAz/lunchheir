# Lunch Heir root ignore. Upstream Lawnchair has its own .gitignore inside upstream/.
.idea/
*.iml
.gradle/
local.properties
build/
.DS_Store

# Source overlay is applied to the submodule working tree at build time; the resulting
# dirty state of upstream/ is expected and should not be committed into the submodule.
